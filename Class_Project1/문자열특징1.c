#include <stdio.h>// 문자열 맨 뒤에는 널이란 정수로 바꾸면 0이되는 글자가 들어있음. 문자열의 끝을 나타냄. void main8_3() // 문자열 스캔 // 문자열===문자열나열됨. 맨 마지막에 마지막 표시인 널이 있음. // 8_3{    char str[5] = "ho"; // 5개 공간이라 [0]->h 에 [1]->o 에 [2]->널/정수로 0/글자로 '\0'     // [3][4] 에는 초기화 갯수가 모자므로. 0으로 채워짐.    printf("%c %c >%c< (2) str[2] = %d 문자열바로뒤에 붙어있는널임//", str[0], str[1], str[2], str[2]);    printf("(3) >%c< str[3] = %d(4) >%c< str[4] = %d\n", str[3], str[3], str[4], str[4]);} // h o >< (2) str[2] = 0 문자열바로뒤에 붙어있는널임//(3) >< str[3]=0    (4) >< str[4]=0// 1. 문자열 뒤에는 항상 Null이 있다!// 2. 문자열 빈 공간에는 항상 0이 채워져 있다!// 실행 안됨...... 한번 싹다 바꿔서 실행해보자 나중에!